{
  "name": "kagent-mcp-server",
  "version": "1.0.0",
  "description": "K-Agent Model Context Protocol Server",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "ts-node src/index.ts",
    "setup": "npm install && npm run build",
    "clean": "rm -rf build node_modules"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "kubernetes",
    "k-agent"
  ],
  "author": "K-Agent Labs",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  }
}
