!!! success "Getting Started Tip"
    Choose your preferred way to run the labs. All platforms are fully supported.

<div class="grid cards" markdown style="text-align: center;border-radius: 20px;">

- 🐳 **Docker Desktop**
  ```bash
  cd labs-environment && docker-compose up -d
  ```

- ☸️ **Minikube**
  ```bash
  minikube start
  ```

- 🌐 **Google Cloud Shell**
  ```bash
  gcloud auth login
  ```

- 🎯 **Kind**
  ```bash
  kind create cluster --name kagent-lab
  ```

</div>
